<HTML><HEAD>
<TITLE>錯誤：欲連結之網站/網頁位置無法正確的傳回</TITLE>
</HEAD><BODY>
<H1>錯誤</H1>
<H2>欲連結之網站/網頁位置無法正確的傳回</H2>
<HR>
<P>
當嘗試傳回下面的位置時：
<A HREF="%U">%U</A>
<P>
發生了下列的錯誤：
<UL>
<LI>
<STRONG>
Valid document was not found in the cache and <code>only-if-cached</code>
directive was specified.
<BR>
要求的文件並未存在於本 PROXY 伺服器上，而且您設定了<code>only-if-cached</code>
指令。
</STRONG>
</UL>

<P>

You have issued a request with a <code>only-if-cached</code> cache control
directive. The document was not found in the cache, <em>or</em> it required
revalidation prohibited by <code>only-if-cached</code> directive.
<BR>
您送出了一個包含<code>only-if-cached</code> cache 控制指令的連結要求。而文件並
未在 PROXY 伺服器發現，<em>或者</em>這個連線要求被<code>only-if-cached</code>
指令評估為禁止存取。
</P>
